<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.prototype">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/asicon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/asicon"
        android:supportsRtl="true"
        android:theme="@style/Theme.Prototype">
        <activity android:name=".AutoCompleteWidget"></activity>
        <activity android:name=".SpinnerWidget" />
        <activity android:name=".DetailIntentActivityWithData" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>